#include <stdio.h>
int main()
{
 	char chr;
  	chr = getchar();
  	chr == 'a' || chr == 'e' || chr == 'i' || chr == 'o' || chr == 'u' || chr == 'A' ||chr == 'E' || chr == 'I' || chr == 'O' || chr == 'U' ? printf("vowel") : printf("consonant");
    return 0;
}