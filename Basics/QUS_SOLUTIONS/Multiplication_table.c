#include <stdio.h>
/* Include other headers as needed */
int main()
{
  	int i,num,term;
  	scanf("%d%d",&num,&term);
  	for(i=1;i <= term;i++)printf("%d\n",num*i);
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    return 0;
}